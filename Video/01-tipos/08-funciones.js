// funcion saludar con () y con {}
function saludar () {
    console.log('hola Dubai');
}
// para llamar  a la funcion: nombre ();
saludar ();

// esta funcion es suma y return es palabra reservada, y se le indica que hacer
function suma() {
    return 2 + 2;
  }
  // return calcula y devuelve el valor 
  // definimos la variable resultado , que será el valor de la funcion suma
 
  //para imprimir tambien se puede poner en el console log
  //la funcion suma con sus()

    console.log(suma());
