window.alert("hello world");
alert("hello world");

const numero1 = 5;
const numero2 = 8;

// se verifica que numero1 es menor o igual que numero2
if (numero1 <= numero2) {
  alert(numero1 + " no es mayor que " + numero2);
}

// se verifica que numero2 es mayor o igual a cero , por lo tanto es positivo
if (numero2 >= 0) {
  alert(numero2 + " es positivo");
}

// se verifica que numero1 es negativo o distinto de cero 
if (numero1 < 0 || numero1 !== 0) {
  alert(numero1 + " es negativo o distinto de cero");
}

